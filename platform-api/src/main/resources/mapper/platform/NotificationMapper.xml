<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.songhg.veri.agent.notification.infrastructure.mapper.NotificationMapper">

    <resultMap id="notificationMap" type="com.songhg.veri.agent.notification.domain.UserNotification">
        <constructor>
            <arg column="id" javaType="java.util.UUID"/>
            <arg column="user_id" javaType="java.util.UUID"/>
            <arg column="type" javaType="java.lang.String"/>
            <arg column="title" javaType="java.lang.String"/>
            <arg column="body" javaType="java.lang.String"/>
            <arg column="link" javaType="java.lang.String"/>
            <arg column="metadata_json" javaType="java.lang.String"/>
            <arg column="read_at" javaType="java.time.Instant"/>
            <arg column="created_by" javaType="java.lang.String"/>
            <arg column="created_at" javaType="java.time.Instant"/>
            <arg column="updated_at" javaType="java.time.Instant"/>
        </constructor>
    </resultMap>

    <sql id="notificationColumns">
        id, user_id, type, title, body, link,
        metadata_json::text as metadata_json,
        read_at, created_by, created_at, updated_at
    </sql>

    <sql id="statusFilter">
        <if test="query.status != null">
            <choose>
                <when test="query.status == 'UNREAD'">
                    and read_at is null
                </when>
                <when test="query.status == 'READ'">
                    and read_at is not null
                </when>
            </choose>
        </if>
    </sql>

    <insert id="insert">
        insert into iam_user_notification (
            id, user_id, type, title, body, link, metadata_json, read_at, created_by, created_at, updated_at
        ) values (
            #{id}, #{userId}, #{type}, #{title}, #{body}, #{link},
            cast(coalesce(#{metadataJson}, '{}') as jsonb),
            #{readAt}, #{createdBy}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <select id="notifications" resultMap="notificationMap">
        select <include refid="notificationColumns"/>
        from iam_user_notification
        where user_id = #{userId}
        <include refid="statusFilter"/>
        order by created_at desc, id desc
        limit #{query.size}
        offset #{query.offset}
    </select>

    <select id="countNotifications" resultType="long">
        select count(1)
        from iam_user_notification
        where user_id = #{userId}
        <include refid="statusFilter"/>
    </select>

    <select id="countUnread" resultType="long">
        select count(1)
        from iam_user_notification
        where user_id = #{userId}
          and read_at is null
    </select>

    <select id="notification" resultMap="notificationMap">
        select <include refid="notificationColumns"/>
        from iam_user_notification
        where id = #{id}
        limit 1
    </select>

    <update id="markRead">
        update iam_user_notification
        set read_at = coalesce(read_at, #{readAt}),
            updated_at = #{readAt}
        where id = #{id}
          and user_id = #{userId}
          and read_at is null
    </update>

    <update id="markAllRead">
        update iam_user_notification
        set read_at = #{readAt},
            updated_at = #{readAt}
        where user_id = #{userId}
          and read_at is null
    </update>
</mapper>
