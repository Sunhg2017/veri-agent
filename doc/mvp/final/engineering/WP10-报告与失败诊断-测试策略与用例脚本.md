# WP10 报告与失败诊断 - 测试策略与用例脚本

| 项目 | 内容 |
|---|---|
| 工作包 | WP10 报告与失败诊断 |
| 角色产出 | 资深质量工程师 |
| 文档性质 | 测试策略、用例矩阵、脚本门禁和准出要求 |
| 当前口径 | 覆盖报告生成、查询、WP9/WP8/WP3/WP5 证据聚合、失败分类、AI 诊断、缺陷草稿、导出脱敏、权限审计、DB validation、前端主链路、M8A-M8F 文档准出和 M9 WP3/WP5 evidence adapter |
| 版本 | v0.1 |
| 日期 | 2026-06-16 |

## 1. 测试目标

1. 验证 WP10 只消费 WP9/WP8/WP3/WP5 脱敏摘要，不读取或输出原始敏感产物。
2. 验证报告生成幂等、状态机、快照、schemaVersion、traceId 和 redaction policy 稳定。
3. 验证规则失败分类在无模型时可用，AI 诊断通过 WP2 策略、预算和脱敏边界。
4. 验证缺陷草稿只保存平台内草稿，不自动写外部缺陷系统。
5. 验证 JSON/Markdown 导出和前端 DOM 不包含禁止字段。
6. 验证权限、项目 scope、审计事件、DB migration 和 quality gate 可作为发布准出入口。

## 2. 测试范围

| 模块 | 覆盖 |
|---|---|
| Health | reporting 开关、limits、schema、redaction policy 和安全摘要。 |
| Report | generate、retry、list、detail、archive、requestKey 幂等和非法状态流。 |
| WP9 adapter | run export 读取、REPORT_HANDOFF 摘要、源 run 未终态、项目越权和脱敏边界。 |
| Evidence | WP8/WP3/WP5/WP9 manifest 聚合、digest、summaryKeys、数量上限和禁止字段扫描。 |
| Classification | errorCode、status、nodeType、runnerMode、timeout、blocked、lease release、trigger 来源。 |
| Diagnosis | context builder、WP2 invocation、预算阻断、provider 错误、fallback 和响应策略。 |
| Defect draft | 创建、审阅、dismiss、payload preview、状态流和审计。 |
| Export | JSON/Markdown、manifest、digest、fieldSetVersion、redaction scan 和阻断。 |
| Frontend | 权限入口、报告生成、详情、诊断、草稿、导出、loading/empty/error 和响应式。 |
| DB | 表、约束、索引、权限 seed、审计字典和 validation。 |

## 3. P0 用例矩阵

| 编号 | 优先级 | 场景 | 期望 |
|---|---|---|---|
| WP10-HEALTH-001 | P0 | 查询 health | 返回开关、limits、schemaVersion、redactionPolicy，不泄露 provider 或 secret。 |
| WP10-REPORT-001 | P0 | 基于 READY execution run 生成报告 | 返回 READY 报告，写 `report.generated` 审计。 |
| WP10-REPORT-002 | P0 | 重复 `executionRunId + requestKey` | 返回同一 report，`idempotentReplay=true`，不重复写证据。 |
| WP10-REPORT-003 | P0 | 源 run 不存在或越权 | 返回 `REPORT_SOURCE_RUN_NOT_FOUND` 或 403，不暴露源 run 细节。 |
| WP10-REPORT-004 | P0 | 源 run 无 `REPORT_HANDOFF` 摘要 | 返回 `REPORT_SOURCE_RUN_NOT_READY`。 |
| WP10-REPORT-005 | P0 | FAILED 报告重试 | 状态按 `FAILED -> QUEUED -> GENERATING` 收敛，保留旧失败审计。 |
| WP10-REPORT-006 | P0 | ARCHIVED 报告触发诊断 | 返回 `REPORT_INVALID_STATE`。 |
| WP10-EVIDENCE-001 | P0 | 聚合 WP8 evidence | 只包含 refs、状态、计数、digest、summary keys 和脱敏账号摘要。 |
| WP10-EVIDENCE-002 | P0 | evidence 包含 secret/token/raw payload 字段 | redaction scan 阻断并写 `report.export.blocked` 或生成失败证据。 |
| WP10-EVIDENCE-003 | P0 | evidence 超过上限 | 裁剪到 maxEvidenceItems，manifest 标记 truncated。 |
| WP10-CLASSIFY-001 | P0 | ASSERTION_FAILED 节点 | 规则分类输出 assertion bucket 和 evidenceRef。 |
| WP10-CLASSIFY-002 | P0 | 下游 BLOCKED 节点 | 分类输出 dependency blocked，不误判为 runner 失败。 |
| WP10-CLASSIFY-003 | P0 | account lease release 失败 | 分类输出 data/account bucket，不泄露 lease token。 |
| WP10-DIAG-000 | P0 | 手动触发诊断但 WP2 不可用 | 返回 `AI_FAILED/REPORT_DIAGNOSIS_POLICY_BLOCKED`，保留规则分类，context 只返回 digest。 |
| WP10-DIAG-001 | P0 | AI 诊断成功 | 输出 `AI_READY`、modelInvocationDigest、rootCauseCandidates、confidence、evidenceRefs、nextActions、manualReviewRequired。 |
| WP10-DIAG-002 | P0 | WP2 预算阻断 | 诊断状态 AI_FAILED，规则分类仍可用，记录 policy blocked 摘要。 |
| WP10-DIAG-003 | P0 | context 中混入 raw prompt/token | context builder 拦截，不调用 WP2。 |
| WP10-DRAFT-001 | P0 | 生成缺陷草稿 | 草稿为 DRAFT，包含标题、复现摘要、影响范围、优先级建议和证据引用。 |
| WP10-DRAFT-002 | P0 | 审阅或 dismiss 草稿 | 状态流合法，写审计。 |
| WP10-EXPORT-001 | P0 | 导出 JSON 摘要 | manifest 包含 schemaVersion、fieldSetVersion、digest、aggregateOnly。 |
| WP10-EXPORT-002 | P0 | 导出 Markdown 摘要 | 内容长度受控，不包含禁止字段。 |
| WP10-PERM-001 | P0 | 无 `report:read` 查询详情 | 403，前端直达显示无权限态。 |
| WP10-PERM-002 | P0 | 无 `report:export` 导出 | 403，前端隐藏导出按钮。 |

## 4. 安全测试

1. 项目越权读取、生成、诊断、导出和草稿操作必须 403。
2. 报告详情、导出内容、审计 payload、模型输入和前端 DOM 不包含 secret、token、cookie、Authorization、password、lease token、`secret://` 原文或完整 secretRef。
3. 不包含 runner stdout/stderr、请求正文、响应正文、webhook payload、截图、视频、源码包、raw prompt、raw response 或 provider error 原文。
4. AI 诊断上下文超过上限时必须裁剪，并输出 contextDigest 和 truncation flag。
5. 脱敏扫描命中禁止字段时，导出状态为 BLOCKED，不返回命中敏感值本身。
6. 缺陷草稿 payload preview 默认 masked，不包含外部系统 token 或真实 webhook 地址。

## 5. 前端测试

| 用例 | 期望 |
|---|---|
| 无权限直达 `#reports` | 显示无权限态，不请求业务数据。 |
| 报告列表空态 | 展示空态和生成入口，按钮受 `report:generate` 控制。 |
| 生成报告缺 executionRunId | 本地校验阻断。 |
| 生成报告成功 | 列表选中报告，详情展示 READY。 |
| 生成报告幂等回放 | 显示回放提示，不重复插入列表项。 |
| 诊断策略阻断 | 保留规则分类，展示 errorCode 和 traceId。 |
| 草稿生成与审阅 | 状态从 DRAFT 到 REVIEWED，按钮 loading 有反馈。 |
| 导出阻断 | 展示 policyCode，不展示敏感命中值。 |
| 390px 移动端 | 无横向溢出，长 digest/traceId 换行，按钮不重叠。 |
| DOM 脱敏扫描 | 页面文本不包含禁止字段样本。 |

## 6. 建议脚本入口

代码阶段新增：

```bash
bash scripts/wp10_report_smoke.sh
bash scripts/wp10_export_redaction_smoke.sh
bash scripts/wp10_diagnosis_quality_eval.sh
bash scripts/wp10_diagnosis_redaction_eval.sh
bash scripts/wp10_quality_gate.sh
```

`wp10_quality_gate.sh` 应串联：

1. 脚本语法检查。
2. `bash scripts/platform_api_java_line_guard.sh`。
3. 后端 WP10 service/controller/OpenAPI/permission/audit 测试。
4. `mvn -B -pl platform-api test` 或定向 WP10 测试加必要跨 WP contract 测试。
5. 前端 WP10 Vitest。
6. `cd portal-web && npm run build`。
7. `bash db/validation/run_wp1_db_validation.sh`。
8. `wp10_report_smoke.sh`，开发模式可 mock 或 managed，release 模式必须 managed。
9. `wp10_export_redaction_smoke.sh`，当前扫描服务端 report detail/export 禁止字段。
10. `wp10_frontend_e2e_smoke.sh`，覆盖桌面/390px 工作台主链路、DOM 禁止字段样本扫描和无横向溢出。
11. `wp10_diagnosis_quality_eval.sh`，离线覆盖 timeout、dependency blocked、runner disabled、账号锁定和 webhook 幂等冲突。
12. `wp10_diagnosis_redaction_eval.sh`，离线构造危险 report summary、evidence manifest 和 rule diagnosis，扫描 WP2 bounded context 禁止字段和值。

## 7. 诊断质量评测

M7A 已新增 `scripts/wp10_diagnosis_quality_eval.sh` 和 `RuleFailureClassifierQualityEvaluationTest`。当前评测直接使用 aggregate-only evidence manifest 调用规则分类器，覆盖 `TIMEOUT`、`DEPENDENCY_BLOCKED`、`RUNNER_FAILURE`、`TEST_DATA_ACCOUNT` 和 webhook 幂等冲突 `UNKNOWN` fallback；该脚本已纳入 `scripts/wp10_quality_gate.sh`，可通过 `WP10_SKIP_DIAGNOSIS_EVAL=1` 显式跳过并在准出说明记录风险。

M7B 已新增 `scripts/wp10_diagnosis_redaction_eval.sh` 和 `ReportDiagnosisContextRedactionEvaluationTest`。当前评测直接构造含危险 key 和危险文本的 report summary、evidence manifest、rule diagnosis，验证 `ReportDiagnosisContextBuilder` 在发送 WP2 前过滤 raw prompt、raw response、runner stdout/stderr、request/response body、webhook payload、secretRef、Authorization、Bearer 和 lease token 等样本；安全策略字段如 `rawPromptIncluded=false` 允许保留。该脚本已纳入 `scripts/wp10_quality_gate.sh`，可通过 `WP10_SKIP_DIAGNOSIS_REDACTION_EVAL=1` 显式跳过并在准出说明记录风险。

| 维度 | P0 阈值建议 |
|---|---|
| 分类命中率 | 离线样例 primary category 命中率不低于 80%。 |
| 安全率 | 诊断输出和上下文禁止字段命中为 0。 |
| 上下文脱敏率 | WP2 bounded context 中危险 key、危险值和原始产物标记命中为 0。 |
| 人工确认 | AI_READY 结果 `manualReviewRequired` 覆盖率 100%。 |
| 证据引用 | 每个候选根因至少包含一个 evidenceRef 或明确说明无证据。 |
| 降级可用 | WP2 不可用时规则分类仍通过主路径测试。 |

## 8. DB Validation

WP10 涉及数据库、权限、审计和模型调用，必须参考 `doc/mvp/final/engineering/WP1-WP4-统一发布准出清单.md`。DB validation 需要覆盖：

1. `report_execution_report`、`report_evidence_manifest`、`report_failure_diagnosis`、`report_defect_draft`、`report_export_manifest` 表存在。
2. 唯一约束、状态 check、外键、索引和时间字段存在。
3. `report:read/generate/diagnose/export/manage` 权限 seed 存在。
4. 默认角色映射符合 PRD。
5. 审计事件字典包含 WP10 事件。
6. schema 中不存在 `tenant_id` 回归。

## 9. 准出标准

1. 报告生成、详情、诊断、草稿和导出主链路可在前端完成。
2. 权限、项目 scope、审计、traceId、schemaVersion 和 redactionPolicy 覆盖主链路。
3. WP9/WP8/WP3/WP5 adapter 不直连跨 WP 表，不读取敏感原文。
4. AI 诊断输入输出均通过脱敏扫描，fallback 可用。
5. 缺陷草稿不写外部系统，payload preview masked。
6. 后端、前端、构建、DB validation、report smoke、redaction smoke、diagnosis eval、diagnosis redaction eval 和 WP10 quality gate 按影响面通过。

## 10. 当前质量结论

当前已推进至 M9 WP3/WP5 evidence adapter 收口阶段。已覆盖 `GET /api/v1/reports/health`、report 权限 seed、基础 DB schema、报告生成/查询、WP9/WP8/WP3/WP5 evidence manifest、WP3 项目 scope 与同项目 trace link 计数、WP5 任务/候选 aggregate-only 证据、`RULE_READY` 规则诊断、无失败 `NO_FAILURE`、账号租借 secondary bucket、`POST /api/v1/reports/{id}/diagnoses`、`GET /api/v1/reports/{id}/diagnoses/latest`、`AI_READY` 模型调用成功、`modelInvocationDigest`、`AI_FAILED/REPORT_DIAGNOSIS_POLICY_BLOCKED` 预算降级、bounded context digest、WP2 bounded context 禁止字段和值专项扫描、`GET /api/v1/reports/{id}/export` JSON/Markdown 脱敏摘要、export manifest digest、`POST /api/v1/reports/{id}/defect-drafts` 平台内草稿、`PATCH /api/v1/reports/{id}/defect-drafts/{draftId}` 审阅状态、masked payload preview、`#reports` 前端入口、API helper、权限入口、报告列表/生成/详情、诊断、草稿和导出面板、桌面/390px Playwright smoke、DOM 禁止字段样本扫描、离线诊断质量评测、离线诊断上下文脱敏评测、前端操作说明、运维 Runbook、发布准出说明、剩余工作盘点、本地准出执行记录、顶层入口文档当前基线同步、OpenAPI contract、DB validation、Java 行数门禁和脱敏安全边界。当前 WP10 范围无剩余 P0 功能开发项；真实 WP2/provider 质量评测看板、外部缺陷系统写入、PDF/Word 完整报告、原始 runner artifact 归档、趋势报表、报告订阅和生产容量承诺均按后续专项推进。任何涉及 Java 生产代码的切片都必须补充对应单测、运行 `scripts/platform_api_java_line_guard.sh`，并在交付说明中写明《阿里巴巴 Java 开发手册》自查结论和核心逻辑注释补充情况。
