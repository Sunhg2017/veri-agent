# 仓库级强制规则

本文件是 Veri Agent 仓库的根级执行规范，适用于所有智能体、自动化助手和人工协作者。子目录如需更细规则，可以新增子目录级 `AGENTS.md`，但不得放宽本文件要求。

## 1. 五角色协作

所有任务必须由以下五个智能体角色按分工协作完成，并在交付说明中留下结论。即使任务看似只涉及单一领域，也必须由五个角色分别给出 `通过`、`有条件通过`、`无影响` 或 `阻断` 结论；存在 `阻断` 时不得声明任务完成。

| 角色 | 强制职责 |
|---|---|
| 资深项目经理 | 拆解任务、控制范围、识别风险、确认里程碑和回滚方式。 |
| 资深产品经理 | 确认需求目标、用户价值、边界条件、非目标范围和验收标准。 |
| 资深服务端架构师 | 评估后端架构、模块归属、API/数据契约、安全、性能、兼容性和跨 WP 影响。 |
| 资深前端工程师 | 评估 UI 路由、菜单/按钮权限、表单校验、loading/empty/error 状态、响应式布局、traceId/错误提示和可测性影响。 |
| 资深质量工程师 | 制定测试策略，确认必要测试、构建、DB validation、smoke 或替代验证已执行并记录结果。 |

任务开始前必须明确目标、范围、非目标、涉及模块、风险、验收标准和回滚方式。任务结束前必须记录五个角色的分工结论、主要变更、验证命令与结果、未执行项及原因。

## 2. 验收定义

只有满足以下条件，任务才算验收完成：

- 需求范围内的代码、文档、配置或脚本变更已经完成。
- 五角色协作结论均为 `通过`、`有条件通过` 或 `无影响`，且没有 `阻断`。
- 已运行与影响面匹配的最小必要验证；涉及公共逻辑、接口、权限、数据库、前端体验、构建配置或跨模块行为时，必须扩大验证范围。
- 相关测试失败不得忽略；必须修复后重跑，或明确说明失败与本任务无关并保留证据。
- 无法运行某项验证时，必须记录原因、风险、影响范围和替代验证方式。
- 涉及 WP1-WP4 发布、数据库、权限、审计、模型调用或文档输入时，必须参考 `doc/mvp/final/engineering/WP1-WP4-统一发布准出清单.md` 或说明不适用原因。

默认验证入口：

```bash
mvn -B -pl platform-api test
cd portal-web && npm test
cd portal-web && npm run build
```

按影响面追加：

```bash
bash db/validation/run_wp1_db_validation.sh
bash scripts/wp1_quality_gate.sh
bash scripts/wp2_quality_gate.sh
bash scripts/wp3_quality_gate.sh
bash scripts/wp4_document_input_smoke.sh
bash scripts/wp4_binary_document_smoke.sh
bash scripts/wp4_ai_parse_quality_eval.sh
```

WP2 当前由 `platform-api` 承载，不得使用历史独立模块命令作为验收入口。

## 3. 验收后自动提交和推送

所有任务验收完成后，必须自动提交变动并推送至远端，除非用户明确禁止提交或推送，或出现本节列出的安全阻断。

提交和推送顺序：

```bash
git status --short
git diff --check
git add <only-related-files>
git commit -m "<scope>: <summary>"
git push
```

提交前必须确认：

- 只纳入本任务相关文件，不得混入无关工作区变更。
- 不得提交密钥、本地私有配置、临时文件、调试输出、构建产物或无关格式化改动。
- 不得覆盖、回退或清理非本任务产生的改动，除非用户明确要求。
- commit message 必须写清楚具体变动内容，禁止只写 `update`、`fix`、`changes` 等模糊描述。

推荐 commit message 格式：

```text
<scope>: <简要说明具体变更>

- 变更了什么
- 为什么变更
- 如何验证
```

可用 scope 包括但不限于 `platform-api`、`portal-web`、`db`、`docs`、`repo-rules`、`wp1`、`wp2`、`wp3`、`wp4`。

推送成功后，最终交付说明必须写明远端分支、commit hash、主要变更和验证结果。

## 4. 阻断与失败处理

出现以下情况时，不得继续自动提交或推送，必须说明原因并给出下一步建议：

- 验收未通过或存在未解决测试失败。
- 工作区存在无法确认归属的无关改动。
- 发现密钥、敏感信息或不应提交的本地文件。
- 用户明确要求不提交或不推送。
- 当前分支没有远端，或远端权限、认证、网络失败。
- push 因非快进或远端拒绝失败。

push 失败时不得强推，除非用户明确授权。非快进失败应先同步远端并重新运行必要检查；无法安全恢复时，保留本地 commit，并说明当前分支、commit hash、失败原因和建议动作。

## 5. 交付说明模板

每次任务完成后，最终回复至少覆盖以下信息：

```text
需求结论：
角色协作结论：
主要变更：
验证命令与结果：
未执行项及原因：
commit：
push：
```
